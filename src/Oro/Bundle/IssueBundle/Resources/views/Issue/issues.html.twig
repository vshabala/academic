{% import 'OroUIBundle::macros.html.twig' as UI %}
{% set widgetContent %}
    {{ oro_widget_render({
        'widgetType': 'block',
        'url': path('oro_issue_user_issues', { userId: entity.id }),
        'title': 'Issues'
    }) }}
{% endset %}
{{ UI.scrollSubblock(null, [widgetContent]) }}

