{% import 'OroUIBundle::macros.html.twig' as UI %}
{{ UI.clientButton({
    'dataUrl': path('issue_create', {'userid': entity.id}),
    'aCss': 'no-hash btn-primary',
    'dataId': entity.id,
    'label': 'oro.issue.actions.create'|trans,
    'title': 'oro.issue.actions.create'|trans,
    'widget' : {
        'type' : 'dialog',
        'multiple' : false,
        'refresh-widget-alias': 'activity-list-widget',
        'options' : {
            'alias': 'task-dialog',
            'dialogOptions' : {
                'title' : 'oro.issue.create_issue'|trans,
                'allowMaximize': true,
                'allowMinimize': true,
                'dblclick': 'maximize',
                'maximizedHeightDecreaseBy': 'minimize-bar',
                'width': 1000
            }
        }
    }
}) }}
